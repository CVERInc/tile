#!/usr/bin/env bash
# Pre-push guard: run the full build + test suite before any push; a failure blocks the push.
# Tracked in-repo (not .git/hooks). Activate once with:  git config core.hooksPath hooks
# Bypass in an emergency with:  git push --no-verify   (CI still guards on the server side).
exec bash "$(dirname "$0")/../scripts/test.sh"
