---
# The locale override half of the category-heading requirement: a translated display name for the
# SAME slug (`press`), read by pages/[...loc]/category/[slug]/index.astro via loadSite(files,
# `${url}/x`) — which content/_site.md's own base value falls back to when this file is absent.
blog-category-names: press=新聞稿
---
