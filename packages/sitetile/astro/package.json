{
  "name": "@tile/sitetile-astro",
  "version": "0.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "description": "sitetile's Astro rendering layer — wraps the framework-free site-core model in per-type Astro components + platform defaults (nav/footer/Lingo/native View Transitions/reef tokens). The REEF with Site production renderer.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "smoke": "node smoke-build.mjs"
  },
  "dependencies": {
    "@cvernet/signet": "latest",
    "astro": "^7.1.3",
    "satori": "^0.12.0",
    "@resvg/resvg-js": "^2.6.2",
    "@fontsource/inter": "^5.1.0",
    "@fontsource/noto-sans-tc": "^5.1.0"
  },
  "devDependencies": {
    "lucide-static": "^1.23.0"
  },
  "overrides": {
    "sharp": "^0.35.0",
    "svgo": "^4.0.2"
  }
}
