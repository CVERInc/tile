---
palette-ink: "#241f1c"
palette-paper: "#f7f3ec"
type-display: serif
---

The structured theme SOURCE. It compiles to theme.css, which the build stages separately — it is
not a content page, and a collector that let it through would route a bogus /_theme and carry the
whole residue block into the pages JSON.
