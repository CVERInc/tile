---
palette-ink: "#241f1c"
palette-paper: "#f7f3ec"
type-display: serif
---

The structured theme SOURCE. The platform compiles it into the theme.css that travels with an
export, and the build stages that file separately — nothing in this repo reads the keys above. It
is not a content page either, and a collector that let it through would route a bogus /_theme and
carry the whole residue block into the pages JSON.
